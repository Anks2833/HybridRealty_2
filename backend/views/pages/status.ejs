<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <style>
      body {
        font-family: system-ui, sans-serif;
        max-width: 800px;
        margin: 0 auto;
        padding: 20px;
        line-height: 1.6;
      }
      .container {
        background: #f9fafb;
        border-radius: 8px;
        padding: 20px;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.05);
      }
      h1 {
        color: #2563eb;
      }
      .status {
        color: #16a34a;
        font-weight: bold;
      }
      .info {
        margin-top: 20px;
        padding-top: 20px;
        border-top: 1px solid #e5e7eb;
      }
      .footer {
        margin-top: 30px;
        font-size: 0.9rem;
        color: #6b7280;
        text-align: center;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1><%= appName %> API</h1>
      <p>Status: <span class="status"><%= status %></span></p>
      <p>Server Time: <%= serverTime %></p>
      <p>Environment: <%= environment %></p>

      <div class="info">
        <p>
          The <%= appName %> API is running properly. This backend serves
          property listings, user authentication, and AI analysis features for
          the <%= appName %> property platform.
        </p>
      </div>

      <div class="footer">
        <p>© <%= currentYear %> <%= appName %>. All rights reserved.</p>
      </div>
    </div>
  </body>
</html>
